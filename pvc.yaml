apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: azurefile-pvc
  namespace: default

spec:
  accessModes:
    - ReadWriteMany

  storageClassName: azurefile-sc

  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      type: azurefile    